# json_dump — Phase 0: Incoming data

Drop every raw file you receive from the source systems into this folder exactly as it arrives. Keep the messy filenames and inconsistent structures for now—we want an untouched snapshot of the original data before we start cleaning anything.
