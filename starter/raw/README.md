# raw — Phase 1: Organized files

Move files here after you normalize their filenames (e.g., consistent casing, spaces to underscores). Do not touch the file contents yet—this stage is only for tidy naming so downstream scripts can reference the files reliably.
