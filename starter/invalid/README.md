# invalid — Phase 2: Rejected data

Whenever a file fails your validation rules (missing fields, bad schema, unreadable data, etc.), park it here along with any notes you need for follow-up. Nothing in this folder moves forward in the pipeline until the issues are fixed.
